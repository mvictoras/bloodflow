-
  action: "add_triggers"
  triggers:
    t1:
      params:
        condition: "cycle() % 1 == 0"
        actions_file : "cinema.yaml"
    t2:
      params:
        condition: "cycle() % 1 == 0"
        actions_file : "trame.yaml"