-
  action: "add_triggers"
  triggers:
    t1:
      params:
        condition: "cycle() >= 50"
        actions_file : "slices.yaml"
    t2:
      params:
        condition: "cycle() >= 50"
        actions_file : "trame.yaml"
